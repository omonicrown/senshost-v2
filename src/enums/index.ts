export enum TimeRuleCadenceEnums {
    MINUTES,
    HOURS,
    DAYS,
    MONTHS,
    YEARS
}
export enum RuleTriggerTYpes {
    dataReceived,
    schedule,
}

export enum RuleDataSouceTypeEnums {
    device,
    aggregateField
}